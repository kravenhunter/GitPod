const Project = () => {
  return (
    <section className='select_project'>
      <h3 className='select_project_top_title'>Select project,</h3>
      <h3 className='select_project_middle_title'>
        check dependencies, checkout branch, view readme.txt, install tools, run build, run test,
      </h3>
      <h3 className='select_project_bottom_title'>start coding.</h3>
    </section>
  );
};

export default Project;
